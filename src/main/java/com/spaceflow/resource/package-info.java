/**
 * Resource module — manages bookable spaces (rooms, desks, capacity, location).
 * Placeholder: to be implemented in the resource iteration following the same
 * hexagonal layout as the {@code booking} module (domain / application / adapters).
 */
package com.spaceflow.resource;
